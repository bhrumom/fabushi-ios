import Foundation

enum SendAcceptanceContract {
    static let nonceDigestMismatch = "send/nonce-digest-mismatch"
    static let hostAccountSlot = "host"
    static let disableSendAcceptReturnEnvironment = "SAND_DISABLE_SEND_ACCEPT_RETURN"
}
